plugins {
    id("testaiproject.android.feature")
}

android {
    namespace = "com.example.test_ai_project.feature.splash"
}

// Compose, Hilt, serialization, navigation, lifecycle, and the permitted core layers all
// come from the feature convention — see AndroidFeatureConventionPlugin.
